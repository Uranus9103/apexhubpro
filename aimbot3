-- FULL SCRIPT: ESP mặc định + Hitbox toggle + tăng/giảm size + GUI + Anti-AFK
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- ========== CẤU HÌNH ==========
local HITBOX_SIZE = Vector3.new(50,50,50) -- mặc định
local HITBOX_COLOR = BrickColor.new("Really red")
local HITBOX_TRANSPARENCY = 0.3
local HITBOX_OFFSET = Vector3.new(0,25,0)

local ESP_Enabled = true
local HITBOX_Enabled = false

local espData = {}
local hitboxData = {}

-- ========== GUI ==========
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HelperGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0,100,0,40)
ToggleButton.Position = UDim2.new(1,-110,1,-50)
ToggleButton.BackgroundColor3 = Color3.fromRGB(40,40,40)
ToggleButton.TextColor3 = Color3.new(1,1,1)
ToggleButton.Text = "Menu"
ToggleButton.Parent = ScreenGui

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0,200,0,200)
Frame.Position = UDim2.new(1,-210,1,-250)
Frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
Frame.Visible = false
Frame.Parent = ScreenGui

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = Frame
UIListLayout.FillDirection = Enum.FillDirection.Vertical
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0,6)

ToggleButton.MouseButton1Click:Connect(function()
    Frame.Visible = not Frame.Visible
end)

local function createButton(text, parent, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1,-10,0,36)
    btn.Position = UDim2.new(0,5,0,0)
    btn.BackgroundColor3 = Color3.fromRGB(60,60,60)
    btn.TextColor3 = Color3.new(1,1,1)
    btn.Font = Enum.Font.SourceSansBold
    btn.Text = text
    btn.Parent = parent
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- ========== ESP ==========
local function createESPForCharacter(player, character)
    if espData[player] then return end
    local head = character:FindFirstChild("Head")
    local humanoid = character:FindFirstChild("Humanoid")
    if not head or not humanoid then return end

    local billboard = Instance.new("BillboardGui")
    billboard.Size = UDim2.new(0,120,0,30)
    billboard.StudsOffset = Vector3.new(0,2,0)
    billboard.AlwaysOnTop = true

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1,0,1,0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(0,255,255)
    label.Text = "[ "..player.Name.." ]"
    label.Font = Enum.Font.SourceSansBold
    label.TextScaled = true
    label.Parent = billboard

    billboard.Parent = head

    local conn
    conn = RunService.RenderStepped:Connect(function()
        if humanoid.Health <=0 then
            billboard.Enabled = false
        else
            billboard.Enabled = true
        end
    end)

    espData[player] = {billboard=billboard, conn=conn}
end

local function ensureESPForPlayer(player)
    player.CharacterAdded:Connect(function(cr)
        task.wait(0.05)
        createESPForCharacter(player, cr)
    end)
    if player.Character then
        createESPForCharacter(player, player.Character)
    end
end

for _,p in ipairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then ensureESPForPlayer(p) end
end
Players.PlayerAdded:Connect(function(p)
    if p ~= LocalPlayer then ensureESPForPlayer(p) end
end)

-- ========== HITBOX ==========
local function createHitboxForCharacter(player, character)
    if hitboxData[player] then return end
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    local part = Instance.new("Part")
    part.Size = HITBOX_SIZE
    part.Anchored = true
    part.CanCollide = false
    part.Material = Enum.Material.Neon
    part.BrickColor = HITBOX_COLOR
    part.Transparency = HITBOX_TRANSPARENCY
    part.Parent = workspace

    local conn
    conn = RunService.RenderStepped:Connect(function()
        if HITBOX_Enabled and hrp.Parent then
            part.CFrame = hrp.CFrame + HITBOX_OFFSET
        else
            part.Transparency = 1
        end
    end)

    hitboxData[player] = {part=part, conn=conn}
end

local function ensureHitboxForPlayer(player)
    player.CharacterAdded:Connect(function(cr)
        task.wait(0.05)
        createHitboxForCharacter(player, cr)
    end)
    if player.Character then
        createHitboxForCharacter(player, player.Character)
    end
end

for _,p in ipairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then ensureHitboxForPlayer(p) end
end
Players.PlayerAdded:Connect(function(p)
    if p ~= LocalPlayer then ensureHitboxForPlayer(p) end
end)

-- ========== GUI BUTTONS ==========
local hitboxToggleBtn = createButton("[OFF] Hitbox", Frame, function()
    HITBOX_Enabled = not HITBOX_Enabled
    hitboxToggleBtn.Text = (HITBOX_Enabled and "[ON] Hitbox" or "[OFF] Hitbox")
end)

createButton("Hitbox +", Frame, function()
    HITBOX_SIZE = HITBOX_SIZE + Vector3.new(10,10,10)
end)
createButton("Hitbox -", Frame, function()
    HITBOX_SIZE = HITBOX_SIZE - Vector3.new(10,10,10)
    if HITBOX_SIZE.X < 1 then HITBOX_SIZE = Vector3.new(1,1,1) end
end)

createButton("Turn ALL OFF", Frame, function()
    HITBOX_Enabled = false
    hitboxToggleBtn.Text = "[OFF] Hitbox"
    for p,_ in pairs(hitboxData) do
        local info = hitboxData[p]
        if info and info.part then pcall(function() info.part:Destroy() end) end
    end
    hitboxData = {}
end)

-- ========== Anti-AFK ==========
LocalPlayer.Idled:Connect(function()
    pcall(function()
        local vu = game:GetService("VirtualUser")
        vu:Button2Down(Vector2.new(0,0), Camera.CFrame)
        task.wait(1)
        vu:Button2Up(Vector2.new(0,0), Camera.CFrame)
    end)
end)
