--// Teleport + Keep Tool Function Script
-- Đặt trong StarterPlayerScripts

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local player = Players.LocalPlayer

-- Tạo GUI
local gui = Instance.new("ScreenGui")
gui.Name = "KeepToolGui"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local button = Instance.new("TextButton")
button.Size = UDim2.new(0,150,0,50)
button.Position = UDim2.new(0.5,-75,0.8,0)
button.BackgroundColor3 = Color3.fromRGB(0,200,100)
button.Text = "🔒 Giữ Tool"
button.Font = Enum.Font.SourceSansBold
button.TextSize = 22
button.TextColor3 = Color3.new(1,1,1)
button.Parent = gui

-- Trạng thái giữ
local holding = false

-- Khi bấm nút
button.MouseButton1Click:Connect(function()
	holding = not holding
	if holding then
		button.Text = "✅ Đang giữ"
		
		-- Anti chết
		local char = player.Character
		if char then
			local ff = Instance.new("ForceField")
			ff.Visible = false
			ff.Parent = char
			game:GetService("Debris"):AddItem(ff, 5) -- ForceField 5 giây
		end
	else
		button.Text = "🔒 Giữ Tool"
	end
end)

-- Vòng lặp giữ tool
task.spawn(function()
	while true do
		task.wait(0.2)
		if holding then
			local char = player.Character
			if char then
				local tool = char:FindFirstChildOfClass("Tool")
				if tool then
					-- Nếu tool có RemoteEvent riêng để bắn / dùng
					if tool:FindFirstChild("ActivateEvent") then
						tool.ActivateEvent:FireServer()
					end
					-- Nếu tool dùng chức năng mặc định Roblox
					if tool:FindFirstChild("Activate") then
						pcall(function() tool:Activate() end)
					end
				end
			end
		end
	end
end)
